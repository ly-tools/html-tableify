{
  "name": "html-tableify",
  "version": "0.0.2",
  "description": "Convert json to HTML table",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test ./node_modules/.bin/_mocha --require should --reporter spec",
    "test-cov": "NODE_ENV=test node ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --require should",
    "test-travis": "NODE_ENV=test node ./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --require should"
  },
  "author": "LingyuCoder",
  "license": "MIT",
  "devDependencies": {
    "istanbul": "~0.4.0",
    "mocha": "~2.3.3",
    "should": "~7.1.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/LingyuCoder/html-tableify"
  },
  "keywords": [
    "html",
    "tableify",
    "json"
  ],
  "bugs": {
    "url": "https://github.com/LingyuCoder/html-tableify/issues"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org/"
  },
  "dependencies": {
    "capitalize": "^1.0.0",
    "lodash.defaults": "^3.1.2",
    "lodash.foreach": "^3.0.3",
    "pretty": "^1.0.0"
  }
}
